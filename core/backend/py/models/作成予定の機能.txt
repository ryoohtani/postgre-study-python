crudアプリの作成

以下追加機能を作成するまえに
main.pyに記載されているselectやupdateなどを行えるコードの作成を行う


追加機能
1.create_db.pyで2つ目のテーブル作成機能 
完了
2.insert_db.pyで2つ目のテーブルのデータ作成機能
3.select文で抽出
4.selectとjoinでテーブル結合
5.select文で抽出
6.csvへ出力
7.1つのテーブル内データを消したら、新規で1つ追加する機能、現状全部のテーブル内のデータを消さないと。再度データを入れれない
8.データが追加されたら古い数字に対して1を足していく。
9.従業員コードは最小値は1、最大値は10。
10.従業員コードの最大値は10。10になったら10番目のデータをdeleteする
11.10番目の削除対象のデータをcsvに出力。ここはデータ保管を目的にしている